# 📘 Day 3 – Sorting Data with ORDER BY 🔽

## 🎯 Objective
Learn how to organize and sort data in SQL using the **ORDER BY** clause, and control the sorting order with **ASC** (ascending) and **DESC** (descending).

## 📚 Topics Covered
- `ORDER BY` clause  
- `ASC` and `DESC` keywords  
- Sorting by multiple columns

## 🧠 Practice Questions

1. **List all patients** sorted by `age` in **descending order**.  
2. **Show all `services_weekly` data** sorted by `week_number` (**ascending**) and `patients_request` (**descending**).  
3. **Display staff members** sorted alphabetically by their `name`.

## 🏆 Daily Challenge

**Question:**  
Retrieve the **top 5 weeks** with the **highest patient refusals** across all services, showing the following columns:
- `week`
- `service`
- `patients_refused`
- `patients_request`
Sort the results by `patients_refused` in **descending** order.

#### 📌 Hashtags
`#IDCWithSQL` `#LearnSQL` `#SQLChallenge` `#DataAnalytics` `#SQLForBeginners` `#GrowthMindset` `#JourneyIntoTech`
